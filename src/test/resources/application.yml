spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none

  datasource:
    username: sa
    password: sa
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
  flyway:
    enabled: false